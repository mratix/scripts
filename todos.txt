# Todo and action list
version="260203-alpha, *incomplete, broken during complete rewrite*, by Mr.AtiX + AI rewrite"

#todo diese datei soll bei "git pull/push" ignoriert werden, sie ist nur lokal beim entwickler vorhanden
#todo struktur einringen
- globale variablen: sollen per default (set -u konform) deklariert werden. der normalfall sieht es vor, dass dort keine Werte zugewiesen sind.
- Zuweisungen sollen im darauffolgenden Abschnitt "variables # (bitte anpassen)" untergebracht sein.
- um die Lauffähigkeit z.B. argumentloser Start zu gewährleisten sollten benötigte Werte im Format variable=${variable:-defaultvalue} ablegt sein.

- Überlegung: ob ein ungewollter start/DAU-Schutz (parameterlos, nicht erfüllte *.conf verarbeitungskette, eigene variablen nicht gesetzt) zu einen geführten Interaktions-/Wizardmodus wechseln sollte: z.B. generiert eine config.conf, welche bereits für die verwendung ausreicht

- hardening für headless-safe (optionen wurden gegeben, *.conf vorhanden) keine interaktiven Abfragen welche das Script hängen lassen könnten, dafür gibt es das $LOGFILE

- verarbeitungskette der möglichen konfigurationsdateien: dist.conf|config.conf -> default.conf -> machine.conf -> $thishost.conf/localhost.conf
berücksichtung dass dabei eine mehrfache verarbeitung mit variable=${variable:-defaultvalue} zum schluss nicht den gewünschten wert liefert.
in jeder *.conf ist ein enabled=true|false gesetzt, welches den import anwenden oder verwerfen soll

- automatisches einlesen und anwenden einer maschinenbezogener konfiguration, wenn z.B. HOST_SERVICE_MAP="hpms1:bitcoind,monerod" gesetzt gelesen wurde UND hpms1.conf vorhanden ist bestätigt es die Konfiguration.




# Errors list

